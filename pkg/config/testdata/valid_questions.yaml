---
version: 1
questions:
  environment:
    description: what is the stack environment
    default_value: stage
    schema:
      type: string
      required: True
      maxLength: 10

  domain:
    description: what is the domain
    default_value: example.com
    schema:
      type: string
      required: true
